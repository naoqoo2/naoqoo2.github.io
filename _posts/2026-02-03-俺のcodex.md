---
title: "俺のcodex"
categories:
  - blog
tags:
  - Codex
  - 俺の
---

直近でまとめた「[俺のAntigravity](/blog/my-antigravity/)」のノリで、Codex CLI の自分用メモを置いておきます。

# サンドボックスでネットワークを許可する

`gh` コマンドで GitHub の PR を見たい時、サンドボックスのネットワークが遮断されていると失敗します。以前は不要だったのにいつからかセキュリティ周り厳しくなっていた。
下記を追記すれば OK でした。

```bash
vi ~/.codex/config.toml
```

```toml
[sandbox_workspace_write]
network_access = true
```

- `sandbox_workspace_write` セクションで `network_access = true` にすると、サンドボックスから外部ネットワークへ出られるようになります。
- これで `gh pr view` や `gh pr checkout` がサンドボックス内で動作するようになりました。

# MCP 設定の詳しい手順

MCP の追加やコマンド運用の詳細は、別途まとめた記事に置きました。手順が必要になったらこちらを参照してください。

- [Codex CLI MCP コマンド活用ガイド](/blog/codex-cli-mcp-command-guide/)

# さいごに

Codex も Antigravity と同じく、まずサンドボックス設定を整えてから使い倒すとストレスが減ります。今後も細かいTipsをこの「俺のcodex」に追記していきます。
